<Window x:Class="WassupClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1.UserControls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Chat Application" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button Content="Registrati" Margin="5"/>
                <Button Content="Accedi" Margin="5"/>
                <Button Content="Esci" Margin="5"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='{}{0:dd/MM/yyyy}'}"
                   VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10"/>

        <ListView Grid.Row="2" Grid.Column="0"></ListView>
        <ContentControl Grid.Row="2" Grid.Column="1" Margin="10">
            <StackPanel>
                <TextBlock Text="Chat" FontWeight="Bold" Margin="0,0,0,10"/>
            </StackPanel>
        </ContentControl>

    </Grid>
</Window>
